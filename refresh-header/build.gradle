apply plugin: 'com.android.library'

android {
    compileSdkVersion 26
    buildToolsVersion "26.0.2"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 23
    }

}

dependencies {
    provided 'com.android.support:support-compat:26.+'
    provided project(':refresh-layout')
}